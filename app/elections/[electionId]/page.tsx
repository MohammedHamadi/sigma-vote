// elections/[electionId] — Election detail
export default function ElectionDetailPage({
  params,
}: {
  params: { electionId: string };
}) {
  return (
    <div>
      <h1>Election Detail</h1>
      {/* TODO: Display election info, candidates, status */}
    </div>
  );
}
