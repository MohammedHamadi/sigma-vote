"use client";

// VoteProgress — Step indicator for voting flow
export function VoteProgress({ currentStep }: { currentStep: number }) {
  return (
    <div>
      {/* TODO: Step progress indicator */}
    </div>
  );
}
