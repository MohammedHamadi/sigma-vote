"use client";

// CandidateSelector — Client component for selecting a candidate
export function CandidateSelector() {
  return (
    <div>
      {/* TODO: Radio/card selection for candidates */}
    </div>
  );
}
