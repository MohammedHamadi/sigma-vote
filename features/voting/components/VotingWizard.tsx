"use client";

// VotingWizard — Multi-step voting flow
export function VotingWizard() {
  return (
    <div>
      {/* TODO: Multi-step wizard: select candidate → encrypt → proof → submit */}
    </div>
  );
}
