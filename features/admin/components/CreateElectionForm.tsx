"use client";

// CreateElectionForm — Form for creating a new election + key ceremony
export function CreateElectionForm() {
  return (
    <form>
      {/* TODO: Election name, description, candidates, start/end dates, key ceremony config */}
    </form>
  );
}
