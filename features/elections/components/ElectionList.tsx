// ElectionList — Server component (fetches + renders cards)
export function ElectionList() {
  return (
    <div>
      {/* TODO: Fetch elections and render ElectionCard for each */}
    </div>
  );
}
